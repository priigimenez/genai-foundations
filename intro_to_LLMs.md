Los LLMs son un subcampo dentro del Deep Learning, correspondiendo a las siglas de "Large Language Models" (modelos grandes/extensos de lenguaje). 
En sí, un "modelo de lenguaje" es un modelo que pretende **predecir** y **generar** un lenguaje creíble, y funciona *estimando la probabilidad de que un token, o una secuencia de tokens, ocurra dentro de una secuencia más larga de tokens.*

Estos *tokens* pueden ser caracteres, palabras, o una secuencia de varias palabras, conforman un oración o un grupo de oraciones. 

El modelo es capaz de estimar 'qué viene a continuación' en una secuencia incompleta, sirviéndose del contexto para calcular la probabilidad de diferentes opciones de tokens que 'completen' esa secuencia. 

(continuará...)
Source: https://developers.google.com/machine-learning/resources/intro-llms?hl=es-419
